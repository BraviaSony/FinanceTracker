import { ErrorPayload, Plugin } from 'vite';

type ConsoleLevel = "log" | "info" | "warn" | "error" | "debug";
interface ReboltOverlayOptions {
    enabled?: boolean;
    levels?: ConsoleLevel[];
    broadcast?: "sender" | "all";
    sendOverlay?: boolean;
    rewriteSourceMaps?: boolean;
    dedupeWindowMs?: number;
    overlayOnConsoleError?: boolean;
    filterError?: (err: ErrorPayload["err"]) => boolean;
    filterConsole?: (payload: ConsolePayload) => boolean;
}
interface ConsolePayload {
    level: ConsoleLevel;
    time: number;
    url?: string;
    userAgent?: string;
    args: unknown[];
}
declare function reboltOverlay(options?: ReboltOverlayOptions): Plugin;

export { reboltOverlay as default };
